__pycache__/
*.pyc
logs/
downloads/
partials/
crawls/
.pytest_cache/
*.egg-info/
.serena/
.claude/
.tokensave/
# rv.log is a real debug capture: it names a live onion service and a real
# organisation on every line. Kept locally as design evidence, never committed.
rv.log
# URLs.txt is the operator's own target list, and crawl mode makes it the seed
# list too. It names live onion services; it is local input, not source.
URLs.txt

# Recon output: every line names a live onion service. Local evidence, not source.
*.jsonl

# rvtree-test/ is a local conformance stack: a multi-GiB archive corpus, docker
# images, a RARLAB trial binary, and (under the onion profile) a hidden service
# private key. All generated, none of it source.
rvtree-test/
